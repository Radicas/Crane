
#include <QColor>

namespace Crane {

namespace COLOR {

    const QColor WHITE  = QColor(Qt::white);
    const QColor RED    = QColor(Qt::red);
    const QColor YELLOW = QColor(Qt::yellow);
    const QColor GRAY   = QColor(Qt::gray);
    const QColor BLUE   = QColor(0, 0, 255, 180);
    const QColor BLACK  = QColor(Qt::black);

}  // namespace COLOR

}  // namespace Crane