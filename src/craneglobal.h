
#include <QColor>

namespace Crane {
namespace COLOR {
    const int COLOR_ALPHA = 150;

    const QColor WHITE  = QColor(0, 0, 0, COLOR_ALPHA);
    const QColor RED    = QColor(255, 0, 0, COLOR_ALPHA);
    const QColor YELLOW = QColor(247, 204, 10, COLOR_ALPHA);
    const QColor GRAY   = QColor(190, 190, 184, COLOR_ALPHA);
    const QColor BLUE   = QColor(127, 137, 245, COLOR_ALPHA);
    const QColor BLACK  = QColor(255, 255, 255, COLOR_ALPHA);

}  // namespace COLOR
}  // namespace Crane